import { Card, CardDescription, CardHeader, CardTitle } from '@vacker/ui';

export default function TasadorPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>En construcción</CardTitle>
        <CardDescription>
          El módulo Tasador de Propiedades está en desarrollo. El esquema de datos y las reglas de
          cálculo ya están listos — el formulario y el resto de las pantallas llegan en los
          próximos sprints.
        </CardDescription>
      </CardHeader>
    </Card>
  );
}
